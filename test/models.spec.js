const expect = require('chai').expect;
const models = require('../models');


// beforeEach(async () => {
//   const rando = await User.create({ 
// 	name: 'pikachu', 
// 	email: 'catchemall@gmail.com' 
// 	})
// })
// describe('User model', () => {
//   it('has a name', () => {
// 	expect(rando.name).to.equal('pikachu');
//   })
// })